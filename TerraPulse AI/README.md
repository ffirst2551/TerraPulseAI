# 🌿 TerraPulse AI

**The Pulse of Mangrove Ecosystems & Blue Carbon Intelligence**

## 📖 เกี่ยวกับโปรเจกต์

TerraPulse AI คือแพลตฟอร์มต้นแบบ (Prototype) ที่สร้างขึ้นเพื่อแก้ปัญหาความยากในการเข้าถึงและวิเคราะห์ข้อมูล Blue Carbon ในป่าชายเลน เราผสานเทคโนโลยี AI เข้ากับแดชบอร์ดที่ใช้งานง่าย เพื่อให้การติดตามสุขภาพของระบบนิเวศเป็นเรื่องที่ทุกคนเข้าถึงได้

## ✨ ฟีเจอร์เด่น (Core Features)

| ฟีเจอร์ | รายละเอียด |
|---|---|
| 🗺️ Interactive Eco-Map | แผนที่ 2 มิติที่แบ่งเป็นโซน (Sectors) สามารถคลิกเพื่อดูข้อมูลเจาะลึกเฉพาะจุดได้ |
| 📊 Dynamic Dashboard | แสดงผลสุขภาพป่า (Health), มวลชีวภาพ (Biomass) และความหลากหลายของสายพันธุ์แบบเรียลไทม์ |
| 🛰️ Multimodal AI Scanner | วิเคราะห์ข้อมูลได้ 2 รูปแบบ: Satellite Imagery (ภาพถ่าย) และ Bio-Acoustics (เสียงธรรมชาติ) |
| 🧬 ResNet Pipeline UI | โชว์ขั้นตอนการประมวลผลของ AI ในระดับ Neural Network ให้เห็นกระบวนการทำงานจริง |
| 🛡️ Polygon Verification | ระบบจำลองการยืนยันความถูกต้องของข้อมูล (Mock Data Integrity) เพื่อความโปร่งใสของ Carbon Credit |

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

### Frontend (Smart & Lightweight)

- Framework: React 18 (UMD via CDN) & Babel Standalone
- Styling: Tailwind CSS (Modern & Responsive UI)
- Icons: Lucide React
- PWA Ready: รองรับการติดตั้งเป็น App บนมือถือผ่าน Service Worker (`sw.js`)

### Backend (Scalable Scaffold)

- Engine: Node.js 20+ พร้อม Fastify (เน้นความเร็วสูง)
- Database: Prisma ORM เชื่อมต่อกับ PostgreSQL
- Queue System: BullMQ + Redis สำหรับประมวลผล AI Background Tasks
- Real-time: Socket.IO สำหรับการส่งข้อมูลเซนเซอร์สด

## 🏗️ โครงสร้างโปรเจกต์ (Project Structure)

```text
.
├── index.html            # ส่วนแสดงผลหลัก (Dashboard/Scanner)
├── manifest.json         # การตั้งค่าสำหรับ Progressive Web App
├── sw.js                 # Service Worker สำหรับ Offline/Caching
├── backend/              # โครงสร้างระบบหลังบ้าน
│   ├── src/              # Logic ของ API และการจัดการ AI
│   ├── prisma/           # Database Schema
│   └── .env.example      # ตัวอย่างการตั้งค่า Environment
└── samples/              # ตัวอย่างไฟล์เสียงและภาพสำหรับทดสอบระบบ
```

## 🚀 วิธีเริ่มใช้งาน (Getting Started)

### สำหรับการทดสอบหน้าบ้าน (Frontend)

เพียงเปิดไฟล์ `index.html` ด้วยเว็บเบราว์เซอร์ทั่วไป หรือใช้ Live Server เพื่อสัมผัสประสบการณ์ที่สมบูรณ์แบบ

### สำหรับการรันระบบหลังบ้าน (Backend)

```bash
# 1. เข้าสู่โฟลเดอร์
cd backend

# 2. ติดตั้ง Dependencies
npm install

# 3. ตั้งค่าฐานข้อมูล
cp .env.example .env
npx prisma generate

# 4. รันระบบ Developer Mode
npm run dev
```

## 🌐 การ Deploy

- Frontend: ใช้ GitHub Actions ร่วมกับ GitHub Pages ตาม Workflow ใน `.github/workflows/`
- Backend: รองรับการทำ Dockerization เพื่อขึ้น Cloud Platform เช่น Railway, Render หรือ Fly.io

## 📋 แผนการพัฒนาในอนาคต (Roadmap)

- [ ] เชื่อมต่อ AI Model (ResNet/AST) แบบ Real Inference
- [ ] ระบบ Web Audio สำหรับรับสัญญาณไมโครโฟนสดในพื้นที่ป่า
- [ ] บันทึกข้อมูล Polygon ลงบน Blockchain เพื่อความโปร่งใส 100%
- [ ] เพิ่มระบบจัดการสิทธิ์ผู้ใช้ (Role-based Access Control)

---

**Note:** โปรเจกต์นี้ถูกออกแบบมาเพื่อ Demo แนวคิด (Proof of Concept) ในการแข่งขัน GreenTech โดยเฉพาะ หากต้องการนำขึ้น Production จริง ควรตั้งค่า Security และ API Management เพิ่มเติม

พัฒนาโดยทีม TerraPulse AI สำหรับการแข่งขัน SPU GreenTech Gen Z Challenge 2026 🌳
